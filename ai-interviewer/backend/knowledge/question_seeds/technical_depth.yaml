dimension: technical_depth
seeds:
  - id: technical_depth.java_transaction_consistency
    version: 1
    title: Java 事务一致性与补偿边界
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [java, backend_systems, spring, transaction, consistency, mysql, compensation]
    direction_tags: [internet_tech]
    role_tags: [java_backend]
    rubric:
      must_cover: [事务边界, 幂等控制, 异步补偿, 对账修复]
      minimum_bar: 能说清哪些步骤必须在本地事务内完成，哪些副作用需要异步补偿和审计。
      stretch: 能处理重试、乱序、重复消息、人工修复和资金类不变量。
    priority: 31
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.java_transaction_consistency.order_payment_boundary
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 订单支付链路包含扣款、订单状态、流水和通知，任一步失败都可能造成不一致。
        question_stem: 你会如何划分订单支付链路的事务边界，并设计失败补偿与对账机制？
        prompt_template: 结合候选人的 Java 后端项目经验，生成一道技术深度题，要求说明 Spring 事务边界、幂等键、异步补偿和对账闭环。
        scenario_skill_tags: [java, spring transaction, mysql, idempotency, compensation]
        resume_anchor_hints: [java, spring, transaction, payment, order, mysql]
        failure_categories: [missing_consistency_boundary, missing_idempotency, missing_edge_case]
        rubric_additions: [说明核心不变量, 说明事务内外副作用边界, 说明重复请求和补偿修复]
        expected_signals: [能定义本地事务内必须原子完成的写入, 能设计业务幂等键和唯一约束, 能说明对账和人工修复入口]
        anti_patterns: [把所有下游调用放进同一个事务, 只依赖前端防重, 失败后没有可追踪记录]
        good_answer_hints: [先固定订单和资金不变量, 再拆同步事务与异步副作用, 最后补审计对账]
        priority: 20
        status: active
      - id: technical_depth.java_transaction_consistency.outbox_compensation
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 主交易成功后需要发送积分、通知和账单事件，多个副作用可能部分失败。
        question_stem: 如果主事务已成功但多个异步副作用可能失败，你会如何设计 outbox、重放和补偿流程？
        prompt_template: 生成一道 Java 后端事务一致性深挖题，要求候选人说明 outbox 写入、消息投递、幂等消费、监控告警和人工兜底。
        scenario_skill_tags: [java, outbox, message queue, idempotency, audit]
        resume_anchor_hints: [outbox, mq, transaction, compensation, java]
        failure_categories: [missing_consistency_boundary, missing_idempotency, missing_observability]
        rubric_additions: [说明 outbox 与主事务的原子关系, 说明消费者幂等和重放, 说明失败可观测与人工修复]
        expected_signals: [能区分主不变量和副作用, 能说明至少一次投递下的幂等消费, 能给出对账和补偿任务]
        anti_patterns: [同步调用所有下游, 副作用失败无记录, 重放导致重复入账]
        good_answer_hints: [先说明 outbox 记录与业务写入同事务, 再说明投递重试和消费幂等, 最后说明补偿闭环]
        priority: 17
        status: active

  - id: technical_depth.java_database_query_performance
    version: 1
    title: Java 数据库查询性能与索引治理
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [java, backend_systems, mysql, indexing, query_optimization, pagination]
    direction_tags: [internet_tech]
    role_tags: [java_backend]
    rubric:
      must_cover: [查询模式, 执行计划, 索引设计, 分页治理]
      minimum_bar: 能基于查询条件和数据量解释索引选择、慢查询定位和分页性能问题。
      stretch: 能结合归档、读写分离、缓存和数据增长趋势给出长期治理路径。
    priority: 28
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.java_database_query_performance.join_pagination
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 后台订单搜索支持多条件筛选、关联用户信息和深分页，数据增长后 P95 延迟明显上升。
        question_stem: 你会如何诊断并优化一个 Java 订单搜索接口的 MySQL 查询性能？
        prompt_template: 结合候选人的 Java 和数据库经验，生成一道技术深度题，要求说明执行计划、复合索引、分页方案和回归验证。
        scenario_skill_tags: [java, mysql, explain, indexing, cursor pagination]
        resume_anchor_hints: [mysql, java, order, index, slow query]
        failure_categories: [shallow_debugging, missing_metrics, missing_tradeoff]
        rubric_additions: [说明 explain 关键字段, 说明复合索引顺序, 说明深分页替代方案]
        expected_signals: [能用执行计划定位全表扫描或回表问题, 能设计覆盖索引或游标分页, 能评估写入成本和回归风险]
        anti_patterns: [盲目加索引, 只说加缓存, 不看数据基数和查询条件]
        good_answer_hints: [先确认查询模式和数据分布, 再设计索引和分页, 最后做压测与慢查询监控]
        priority: 17
        status: active
      - id: technical_depth.java_database_query_performance.hot_partition
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 某些租户订单量远高于平均值，按 tenant_id 的查询和写入都集中到少数热点范围。
        question_stem: 如果 MySQL 出现租户热点导致查询和写入都变慢，你会如何定位并治理？
        prompt_template: 生成一道 Java 后端数据库性能深挖题，要求候选人说明热点证据、索引与分片取舍、归档策略和迁移风险。
        scenario_skill_tags: [java, mysql, hot partition, sharding, archive]
        resume_anchor_hints: [tenant, mysql, sharding, indexing, java]
        failure_categories: [shallow_debugging, missing_metrics, missing_tradeoff]
        rubric_additions: [说明热点指标和数据分布, 说明分片或归档的取舍, 说明迁移兼容和回滚]
        expected_signals: [能用慢查询和数据库指标定位热点, 能评估分片带来的查询复杂度, 能设计灰度迁移方案]
        anti_patterns: [只加缓存不解决写热点, 忽略历史数据归档, 不考虑跨分片查询]
        good_answer_hints: [先量化热点租户和表增长, 再比较索引归档分片方案, 最后规划迁移验证]
        priority: 15
        status: active

  - id: technical_depth.java_concurrency_runtime
    version: 1
    title: Java 并发运行时与线程池治理
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [java, jvm, juc, thread_pool, concurrency, backpressure]
    direction_tags: [internet_tech]
    role_tags: [java_backend]
    rubric:
      must_cover: [线程池参数, 队列背压, 锁竞争, JVM 观测]
      minimum_bar: 能解释线程池、锁和异步执行对接口稳定性与资源隔离的影响。
      stretch: 能结合压测、线程栈、指标和降级策略形成运行时治理闭环。
    priority: 26
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.java_concurrency_runtime.thread_pool_backpressure
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个 Java 服务把外部调用放进线程池并行处理，流量升高后队列堆积、超时和拒绝同时出现。
        question_stem: 你会如何分析并治理 Java 线程池队列堆积导致的接口超时问题？
        prompt_template: 结合候选人的 Java 后端经验，生成一道并发运行时题，要求说明线程池参数、队列、拒绝策略、背压和监控。
        scenario_skill_tags: [java, thread pool, queue, timeout, backpressure]
        resume_anchor_hints: [java, thread pool, async, timeout, juc]
        failure_categories: [missing_metrics, shallow_debugging, missing_tradeoff]
        rubric_additions: [说明线程池参数含义, 说明队列水位和拒绝策略, 说明背压或降级边界]
        expected_signals: [能分析吞吐和耗时关系, 能设置隔离线程池和队列上限, 能给出监控和告警指标]
        anti_patterns: [无限扩大线程数, 使用无界队列, 忽略下游超时预算]
        good_answer_hints: [先量化处理能力和排队时间, 再调参数与隔离, 最后增加背压和降级]
        priority: 16
        status: active
      - id: technical_depth.java_concurrency_runtime.jvm_lock_contention
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 高峰期接口 CPU 不高但响应很慢，线程栈显示大量线程等待同一把锁。
        question_stem: 面对 Java 服务中的锁竞争和吞吐下降，你会如何定位、验证并改造？
        prompt_template: 生成一道 Java 并发深挖题，要求候选人说明线程 dump、锁粒度、并发容器、数据一致性和回归验证。
        scenario_skill_tags: [java, lock contention, jvm, juc, concurrency]
        resume_anchor_hints: [java, jvm, lock, concurrency, thread dump]
        failure_categories: [shallow_debugging, missing_metrics, missing_edge_case]
        rubric_additions: [说明线程栈和锁等待证据, 说明锁粒度或并发结构改造, 说明一致性和压测验证]
        expected_signals: [能用 jstack 或 profiler 定位阻塞, 能比较锁拆分与无锁结构, 能验证正确性和性能收益]
        anti_patterns: [只凭感觉改 synchronized, 不验证并发正确性, 忽略异常路径释放资源]
        good_answer_hints: [先拿线程栈证据, 再缩小锁范围或重构状态, 最后做并发测试和压测]
        priority: 14
        status: active

  - id: technical_depth.frontend_runtime_mechanics
    version: 1
    title: 前端运行时机制与性能取舍
    dimension: technical_depth
    job_levels: [junior, mid, senior, staff]
    skill_tags: [frontend, javascript, browser, performance, rendering]
    direction_tags: [internet_tech]
    role_tags: [frontend_web]
    rubric:
      must_cover: [浏览器渲染, JavaScript 执行, 性能瓶颈, 验证方法]
      minimum_bar: 能解释一个真实前端性能问题背后的运行时机制和定位方法。
      stretch: 能把框架机制、浏览器管线、构建产物和线上观测联系起来。
    priority: 24
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.frontend_runtime_mechanics.long_task
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 页面交互出现明显卡顿，Performance 面板显示主线程存在多个 long task。
        question_stem: 请说明你会如何定位并优化一个由主线程 long task 导致的前端卡顿问题。
        prompt_template: 结合候选人的前端项目经验，生成一道技术深度题，要求说明浏览器主线程、渲染管线、定位工具和优化方案。
        scenario_skill_tags: [javascript, long task, rendering, performance]
        resume_anchor_hints: [performance, react, vue, browser, long task]
        failure_categories: [shallow_debugging, vague_metrics, missing_tradeoff]
        rubric_additions: [说明主线程阻塞来源, 说明定位工具和指标, 说明拆分任务或减少渲染成本]
        expected_signals: [能解释事件循环和渲染阻塞, 能使用 Performance 或 RUM 指标, 能提出可验证优化]
        anti_patterns: [只说加防抖, 不看性能证据, 不区分网络慢和渲染慢]
        good_answer_hints: [先复现和采样, 再定位主线程任务, 最后选择拆分计算或减少渲染]
        priority: 16
        status: active
      - id: technical_depth.frontend_runtime_mechanics.bundle_hydration
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 首屏资源体积较大，SSR 页面 hydration 期间交互延迟明显。
        question_stem: SSR 页面首屏可见但不可交互时，你会如何分析 bundle、hydration 和交互延迟？
        prompt_template: 生成一道前端运行时深挖题，要求候选人说明 bundle 分析、hydration 成本、交互指标和优化边界。
        scenario_skill_tags: [ssr, hydration, bundle splitting, interaction latency]
        resume_anchor_hints: [ssr, nextjs, bundle, hydration, frontend]
        failure_categories: [vague_metrics, missing_tradeoff, missing_observability]
        rubric_additions: [说明 LCP 与 INP 差异, 说明代码拆分和延迟 hydration, 说明监控验证方式]
        expected_signals: [能区分可见和可交互, 能分析 JS 体积与执行成本, 能权衡 SSR 与客户端渲染]
        anti_patterns: [只压缩图片, 不分析 JS 执行, 不定义交互指标]
        good_answer_hints: [先拆指标, 再看资源和执行瀑布, 最后给渐进加载方案]
        priority: 14
        status: active

  - id: technical_depth.sre_linux_kubernetes_foundation
    version: 1
    title: SRE Linux 与 Kubernetes 基础深度
    dimension: technical_depth
    job_levels: [junior, mid, senior, staff]
    skill_tags: [sre, linux, kubernetes, networking, observability]
    direction_tags: [internet_tech]
    role_tags: [sre]
    rubric:
      must_cover: [系统资源, 网络链路, 容器调度, 观测证据]
      minimum_bar: 能用系统和平台层面的证据定位一个稳定性问题。
      stretch: 能把节点、容器、服务发现、网络和监控链路串起来分析。
    priority: 24
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.sre_linux_kubernetes_foundation.pod_restart
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: Kubernetes 集群中某服务 Pod 间歇性重启，应用日志只看到连接中断。
        question_stem: 请说明你会如何从 Kubernetes、Linux 资源和应用指标定位 Pod 间歇性重启问题。
        prompt_template: 结合候选人的 SRE 或运维经验，生成一道技术深度题，要求说明排查顺序、关键命令、指标和可能根因。
        scenario_skill_tags: [kubernetes, pod restart, linux, metrics]
        resume_anchor_hints: [kubernetes, pod, linux, metrics, sre]
        failure_categories: [shallow_debugging, missing_observability, missing_action]
        rubric_additions: [说明事件和重启原因, 说明资源限制与 OOM, 说明应用和节点指标对照]
        expected_signals: [能查看 event 和 container status, 能区分 OOM 与探针失败, 能结合节点资源判断]
        anti_patterns: [只重启服务, 不看事件, 不区分应用和平台原因]
        good_answer_hints: [先看事件和状态, 再看资源与探针, 最后关联应用日志和节点指标]
        priority: 16
        status: active
      - id: technical_depth.sre_linux_kubernetes_foundation.network_timeout
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 服务间调用偶发超时，只有跨节点流量明显更容易触发。
        question_stem: 如果服务跨节点调用偶发超时，你会如何从 DNS、Service、网络策略和节点层定位？
        prompt_template: 生成一道 SRE 网络深挖题，要求候选人说明 Kubernetes 服务发现、网络路径、超时证据和隔离实验。
        scenario_skill_tags: [kubernetes networking, dns, service discovery, timeout]
        resume_anchor_hints: [networking, dns, service, kubernetes, timeout]
        failure_categories: [missing_observability, shallow_debugging, missing_edge_case]
        rubric_additions: [说明 DNS 和 Service 检查, 说明跨节点网络路径, 说明抓包或隔离验证]
        expected_signals: [能设计分层排查路径, 能用指标或抓包验证假设, 能区分客户端超时和网络丢包]
        anti_patterns: [只调大超时, 不分层定位, 不做对照实验]
        good_answer_hints: [先确认影响范围, 再拆 DNS Service 网络路径, 最后做隔离和抓包]
        priority: 14
        status: active
  - id: technical_depth.ai_agent_llm_tool_boundaries
    version: 1
    title: Agent LLM 与工具边界
    dimension: technical_depth
    job_levels: [junior, mid, senior, staff]
    skill_tags: [llm, agent, tool calling, rag, prompt]
    direction_tags: [internet_tech]
    role_tags: [ai_agent]
    rubric:
      must_cover: [模型能力边界, tool schema, 错误恢复, 安全约束]
      minimum_bar: 能说明 Agent 何时依赖模型推理、何时应交给工具或确定性代码处理。
      stretch: 能把工具调用、权限、幂等、观测和评估闭环串成一套工程边界。
    priority: 28
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_agent_llm_tool_boundaries.tool_schema
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个客服 Agent 需要查询订单、发起退款和生成回复，工具调用失败会影响真实用户。
        question_stem: 你会如何设计 Agent 的工具 schema、参数校验和失败处理边界？
        prompt_template: 结合候选人的 Agent 或后端集成经验，生成一道技术深度题，要求说明 LLM 与工具调用的职责边界。
        scenario_skill_tags: [agent, tool calling, schema, validation]
        resume_anchor_hints: [agent, tool, function calling, api, llm]
        failure_categories: [missing_edge_case, missing_action, shallow_debugging]
        rubric_additions: [说明工具 schema 设计, 说明参数校验和权限边界, 说明失败重试和人工兜底]
        expected_signals: [能区分模型生成与确定性工具执行, 能提到幂等和权限, 能设计失败恢复路径]
        anti_patterns: [让模型直接改业务状态, 不校验工具参数, 工具失败后只要求重试]
        good_answer_hints: [先定义动作风险等级, 再设计 schema 和校验, 最后补观测与兜底]
        priority: 18
        status: active
      - id: technical_depth.ai_agent_llm_tool_boundaries.prompt_injection
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: Agent 会读取用户上传文档并调用内部工具，文档里可能包含诱导模型越权的内容。
        question_stem: 如果 Agent 面临 prompt injection 和工具越权风险，你会如何在技术链路上做隔离和防护？
        prompt_template: 生成一道 AI Agent 安全边界深挖题，要求候选人说明上下文隔离、工具权限和风险观测。
        scenario_skill_tags: [agent, prompt injection, tool permission, security]
        resume_anchor_hints: [agent, security, prompt, tool calling, rag]
        failure_categories: [missing_edge_case, missing_tradeoff, missing_observability]
        rubric_additions: [说明不可信上下文隔离, 说明工具权限最小化, 说明风险日志和拦截策略]
        expected_signals: [能区分指令层级, 能限制敏感工具调用, 能设计审计和人工确认]
        anti_patterns: [相信所有检索文本, 只靠提示词声明安全, 没有工具权限分级]
        good_answer_hints: [先标记不可信输入, 再做工具授权, 最后补审计和评估用例]
        priority: 16
        status: active
  - id: technical_depth.ai_fullstack_ai_app_integration
    version: 1
    title: AI 全栈应用集成基础
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [ai fullstack, llm, react, python, api]
    direction_tags: [internet_tech]
    role_tags: [ai_fullstack]
    rubric:
      must_cover: [模型 API, 前后端状态, 异步任务, 错误处理]
      minimum_bar: 能说明 AI 应用从前端交互到后端模型调用的关键链路。
      stretch: 能兼顾流式响应、任务状态、权限、成本和失败重试。
    priority: 26
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_fullstack_ai_app_integration.streaming_response
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个 AI 写作应用需要把模型输出实时展示给用户，同时支持取消和错误提示。
        question_stem: 你会如何设计前端到后端的 LLM 流式响应、取消和错误处理链路？
        prompt_template: 结合候选人的全栈项目经验，生成一道 AI 应用集成题，要求说明前端状态、后端接口和模型调用边界。
        scenario_skill_tags: [ai fullstack, llm, streaming, react, api]
        resume_anchor_hints: [llm, streaming, react, api, python]
        failure_categories: [missing_edge_case, missing_action, vague_metrics]
        rubric_additions: [说明流式协议选择, 说明取消和超时处理, 说明前端状态一致性]
        expected_signals: [能提到 SSE 或 WebSocket 取舍, 能处理部分输出和错误, 能避免重复计费或重复任务]
        anti_patterns: [只等完整结果返回, 没有取消语义, 前端状态不可恢复]
        good_answer_hints: [先定义交互状态机, 再设计流式接口, 最后补失败和重试]
        priority: 17
        status: active
      - id: technical_depth.ai_fullstack_ai_app_integration.async_job
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: AI 报告生成可能运行数分钟，用户可能刷新页面或重复提交。
        question_stem: 面对长耗时 AI 任务，你会如何设计任务状态、幂等提交和结果回查？
        prompt_template: 生成一道 AI 全栈异步任务深挖题，要求说明任务模型、幂等、进度展示和失败恢复。
        scenario_skill_tags: [ai fullstack, async job, idempotency, task status]
        resume_anchor_hints: [async, queue, llm, task, fullstack]
        failure_categories: [missing_edge_case, missing_tradeoff, missing_observability]
        rubric_additions: [说明任务 ID 和状态机, 说明幂等和重复提交, 说明进度和失败回查]
        expected_signals: [能设计 pending running failed done 状态, 能处理刷新和重复提交, 能记录模型调用和成本]
        anti_patterns: [请求一直阻塞, 刷新后丢失任务, 重复提交导致多次扣费]
        good_answer_hints: [先建任务表和幂等键, 再设计状态轮询或推送, 最后补观测和补偿]
        priority: 15
        status: active
  - id: technical_depth.mobile_runtime_lifecycle
    version: 1
    title: 移动端运行时与生命周期
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [mobile, android, ios, flutter, lifecycle]
    direction_tags: [internet_tech]
    role_tags: [mobile]
    rubric:
      must_cover: [生命周期, UI 渲染, 后台限制, 资源释放]
      minimum_bar: 能说明移动端页面和应用生命周期对稳定性与性能的影响。
      stretch: 能结合端侧缓存、网络、权限和系统限制设计可靠方案。
    priority: 25
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.mobile_runtime_lifecycle.background_resume
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 移动 App 从后台恢复后页面数据偶发过期，部分请求也会重复触发。
        question_stem: 你会如何处理移动端从后台恢复时的数据刷新、请求去重和 UI 状态恢复？
        prompt_template: 结合候选人的移动端项目经验，生成一道生命周期技术深度题，要求说明后台恢复、缓存和请求控制。
        scenario_skill_tags: [mobile, app lifecycle, cache, request dedupe]
        resume_anchor_hints: [android, ios, flutter, lifecycle, mobile]
        failure_categories: [missing_edge_case, missing_action, shallow_debugging]
        rubric_additions: [说明生命周期事件, 说明缓存新鲜度判断, 说明请求去重和 UI 恢复]
        expected_signals: [能处理前后台切换, 能避免重复请求, 能区分本地状态和远端状态]
        anti_patterns: [每次恢复全量刷新, 不处理并发请求, 忽略系统回收]
        good_answer_hints: [先定义页面状态, 再做缓存策略, 最后处理恢复和重试]
        priority: 16
        status: active
      - id: technical_depth.mobile_runtime_lifecycle.render_jank
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 一个信息流页面滑动卡顿，列表包含图片、视频和复杂组件。
        question_stem: 你会如何从渲染、数据加载和资源管理角度定位移动端列表卡顿？
        prompt_template: 生成一道移动端性能深挖题，要求候选人说明渲染链路、列表复用、图片加载和指标观测。
        scenario_skill_tags: [mobile, rendering, list performance, jank]
        resume_anchor_hints: [mobile, list, performance, rendering, image]
        failure_categories: [missing_metrics, shallow_debugging, missing_observability]
        rubric_additions: [说明帧率和卡顿指标, 说明列表复用和懒加载, 说明图片视频资源控制]
        expected_signals: [能用 profiler 定位主线程耗时, 能优化列表复用, 能控制图片解码和缓存]
        anti_patterns: [只减少页面元素, 不看性能指标, 忽略低端设备]
        good_answer_hints: [先量化卡顿, 再拆渲染和数据加载, 最后做低端机验证]
        priority: 15
        status: active
  - id: technical_depth.ai_algorithm_model_foundation
    version: 1
    title: 算法模型基础与工程实现
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [ai algorithm, model, pytorch, tensorflow, training]
    direction_tags: [internet_tech]
    role_tags: [ai_algorithm]
    rubric:
      must_cover: [模型目标, 数据特征, 训练验证, 工程约束]
      minimum_bar: 能说明模型训练和推理链路中的核心技术要素。
      stretch: 能把算法效果、数据质量、推理性能和线上反馈联动起来。
    priority: 26
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_algorithm_model_foundation.recommendation_model
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 推荐模型线上点击率下降，候选人需要解释模型输入、训练和验证链路。
        question_stem: 你会如何分析一个推荐模型从特征、训练到线上推理的关键技术环节？
        prompt_template: 结合候选人的算法项目经验，生成一道模型基础题，要求说明数据、特征、训练和推理链路。
        scenario_skill_tags: [ai algorithm, model, feature, training]
        resume_anchor_hints: [model, recommendation, feature, pytorch, tensorflow]
        failure_categories: [vague_metrics, missing_tradeoff, shallow_debugging]
        rubric_additions: [说明目标和特征, 说明训练验证切分, 说明线上推理和监控]
        expected_signals: [能解释数据分布和特征质量, 能区分离线和线上指标, 能说明推理延迟约束]
        anti_patterns: [只说调参, 不讨论数据, 不区分训练和线上]
        good_answer_hints: [先定义目标指标, 再看数据特征, 最后连接训练验证和线上]
        priority: 16
        status: active
      - id: technical_depth.ai_algorithm_model_foundation.inference_performance
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 模型效果达标但推理延迟过高，影响在线业务体验。
        question_stem: 如果模型推理延迟过高，你会从模型、服务和硬件资源哪些方向优化？
        prompt_template: 生成一道算法工程性能深挖题，要求候选人说明模型压缩、批处理、缓存和服务化取舍。
        scenario_skill_tags: [ai algorithm, inference, latency, model optimization]
        resume_anchor_hints: [inference, latency, model, gpu, serving]
        failure_categories: [missing_tradeoff, missing_metrics, missing_edge_case]
        rubric_additions: [说明延迟指标拆解, 说明模型和服务优化, 说明效果损失和资源成本取舍]
        expected_signals: [能拆分模型计算和服务开销, 能提到量化蒸馏或缓存, 能评估效果和成本]
        anti_patterns: [只加机器, 不评估效果损失, 不看 P95 延迟]
        good_answer_hints: [先量化瓶颈, 再选择模型或服务优化, 最后做效果回归]
        priority: 15
        status: active
  - id: technical_depth.ai_algorithm_evaluation_metrics
    version: 1
    title: 算法评估指标与实验设计基础
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [ai algorithm, evaluation, metric, experiment, dataset]
    direction_tags: [internet_tech]
    role_tags: [ai_algorithm]
    rubric:
      must_cover: [离线指标, 在线指标, 数据切分, 偏差风险]
      minimum_bar: 能说明模型评估指标如何服务业务目标和实验判断。
      stretch: 能识别数据泄漏、样本偏差、指标代理失真和实验显著性问题。
    priority: 25
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_algorithm_evaluation_metrics.offline_online_gap
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 模型离线 AUC 提升明显，但灰度后业务转化没有改善。
        question_stem: 你会如何分析算法模型离线指标提升但线上业务指标不涨的问题？
        prompt_template: 结合候选人的算法评估经验，生成一道指标和实验设计题，要求说明离线线上差异。
        scenario_skill_tags: [ai algorithm, evaluation, offline metric, online metric]
        resume_anchor_hints: [auc, evaluation, experiment, metric, model]
        failure_categories: [vague_metrics, missing_tradeoff, shallow_debugging]
        rubric_additions: [说明指标代理关系, 说明样本和分布差异, 说明灰度实验验证]
        expected_signals: [能识别离线指标不等于业务目标, 能检查数据分布, 能设计线上实验]
        anti_patterns: [只相信离线分数, 不看业务漏斗, 不排查样本偏差]
        good_answer_hints: [先对齐业务指标, 再查离线数据和线上流量, 最后做实验归因]
        priority: 16
        status: active
      - id: technical_depth.ai_algorithm_evaluation_metrics.data_leakage
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 新模型离线表现异常好，上线后迅速回落，怀疑存在数据泄漏或验证偏差。
        question_stem: 如果你怀疑模型评估存在数据泄漏，你会如何验证和修正实验流程？
        prompt_template: 生成一道算法评估深挖题，要求候选人说明数据泄漏、切分策略和验证集设计。
        scenario_skill_tags: [ai algorithm, data leakage, validation, dataset]
        resume_anchor_hints: [dataset, validation, leakage, experiment, model]
        failure_categories: [missing_edge_case, missing_metrics, shallow_debugging]
        rubric_additions: [说明泄漏来源, 说明时间或用户维度切分, 说明重新验证和回归]
        expected_signals: [能检查标签穿越和重复样本, 能设计时间切分, 能重建可信验证集]
        anti_patterns: [只重新训练, 不查数据链路, 忽略线上反馈]
        good_answer_hints: [先列泄漏假设, 再审查数据生成, 最后重跑验证和线上对照]
        priority: 15
        status: active
  - id: technical_depth.architect_distributed_risk
    version: 1
    title: 架构师分布式风险识别
    dimension: technical_depth
    job_levels: [senior, staff]
    skill_tags: [architect, distributed systems, consistency, scalability, risk]
    direction_tags: [internet_tech]
    role_tags: [architect]
    rubric:
      must_cover: [一致性, 可用性, 容量, 故障边界]
      minimum_bar: 能识别复杂分布式方案中的关键技术风险和验证路径。
      stretch: 能把风险拆成架构约束、组织协作、演进成本和应急预案。
    priority: 27
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.architect_distributed_risk.cross_region_consistency
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 核心业务计划做多地域部署，但订单状态、库存和支付回调存在一致性要求。
        question_stem: 你会如何识别多地域架构中的一致性、可用性和故障恢复风险？
        prompt_template: 结合候选人的架构经验，生成一道分布式风险识别题，要求说明技术约束和验证路径。
        scenario_skill_tags: [architect, distributed systems, consistency, availability]
        resume_anchor_hints: [architecture, distributed, consistency, multi region, risk]
        failure_categories: [missing_tradeoff, missing_failure_mode, vague_scale]
        rubric_additions: [说明一致性边界, 说明故障域和恢复目标, 说明验证和演练方法]
        expected_signals: [能区分强一致和最终一致, 能识别跨地域延迟, 能设计故障演练]
        anti_patterns: [只说多活, 不定义数据边界, 不考虑故障切换]
        good_answer_hints: [先划分业务不变量, 再定义故障域, 最后设计验证和演练]
        priority: 17
        status: active
      - id: technical_depth.architect_distributed_risk.hotspot_scalability
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 平台增长后出现热点租户和热点数据，单点优化很难继续支撑。
        question_stem: 面对热点租户和热点数据，你会如何从架构层识别瓶颈并设计长期治理方案？
        prompt_template: 生成一道架构师技术深度题，要求候选人说明热点识别、分片、隔离和演进成本。
        scenario_skill_tags: [architect, scalability, hotspot, sharding]
        resume_anchor_hints: [architecture, scalability, sharding, hotspot, distributed]
        failure_categories: [vague_scale, missing_tradeoff, missing_metrics]
        rubric_additions: [说明热点指标, 说明隔离和分片策略, 说明迁移和兼容成本]
        expected_signals: [能用指标定位热点, 能比较水平扩展方案, 能考虑迁移风险]
        anti_patterns: [只加缓存, 不看数据分布, 不设计迁移路径]
        good_answer_hints: [先量化热点, 再比较隔离方案, 最后规划灰度迁移]
        priority: 15
        status: active

  - id: technical_depth.ai_agent_tool_schema_validation
    version: 1
    title: Agent 工具 schema 校验与失败处理
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [agent, tool calling, schema, validation, llm]
    direction_tags: [internet_tech]
    role_tags: [ai_agent]
    rubric:
      must_cover: [schema 校验, 参数纠错, 失败分类, 重试边界]
      minimum_bar: 能说明 Agent 工具调用的输入校验、错误码分类和重试策略。
      stretch: 能结合幂等、人工兜底、安全审计形成完整失败处理闭环。
    priority: 27
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_agent_tool_schema_validation.input_validation
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: Agent 调用退款工具时模型偶尔生成错误的金额或币种，工具直接拒绝后用户体验差。
        question_stem: 你会如何设计 Agent 工具调用的输入 schema 校验、参数纠错和失败处理流程？
        prompt_template: 结合候选人的 Agent 或后端集成经验，生成一道技术深度题，要求说明 schema 校验、参数纠错和失败分类。
        scenario_skill_tags: [agent, tool calling, schema, validation, retry]
        resume_anchor_hints: [agent, tool calling, schema, function calling, llm]
        failure_categories: [missing_edge_case, shallow_debugging, missing_action]
        rubric_additions: [说明 schema 校验位置, 说明模型纠错与拒绝边界, 说明失败分类与重试策略]
        expected_signals: [能区分 schema 层与业务层校验, 能让模型在校验失败时自我纠正, 能定义可重试与不可重试错误]
        anti_patterns: [只信任模型输出, 所有错误都重试, 校验失败直接抛给用户]
        good_answer_hints: [先定义工具契约, 再做分层校验, 最后定失败分类与回退]
        priority: 18
        status: active
      - id: technical_depth.ai_agent_tool_schema_validation.error_recovery
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 退款工具偶尔返回 5xx 或部分成功，Agent 无法判断是否需要重试或人工介入。
        question_stem: 如果工具返回模糊错误码或部分成功，你会如何在 Agent 链路里设计恢复与人工 hand-off？
        prompt_template: 生成一道 Agent 工具失败恢复深挖题，要求候选人说明错误码语义、幂等键、补偿和人工 hand-off。
        scenario_skill_tags: [agent, tool calling, idempotency, compensation, hand-off]
        resume_anchor_hints: [agent, idempotency, compensation, retry, hand-off]
        failure_categories: [missing_edge_case, missing_action, missing_observability]
        rubric_additions: [说明错误码语义梳理, 说明幂等与补偿设计, 说明何时升级为人工]
        expected_signals: [能定义可重试与可补偿错误, 能用幂等键避免重复执行, 能给出人工 hand-off 触发条件]
        anti_patterns: [所有 5xx 都重试, 不记录失败现场, 无升级人工的路径]
        good_answer_hints: [先建错误分级, 再做幂等与补偿, 最后定义 hand-off]
        priority: 16
        status: active

  - id: technical_depth.ai_agent_context_compression
    version: 1
    title: Agent 上下文压缩与裁剪
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [agent, context, compression, summary, llm]
    direction_tags: [internet_tech]
    role_tags: [ai_agent]
    rubric:
      must_cover: [token 预算, 摘要策略, 检索召回, 关键信息保留]
      minimum_bar: 能说明 Agent 上下文增长时如何在 token 预算与信息完整之间取舍。
      stretch: 能结合分层摘要、向量检索、缓存与评估闭环形成长会话稳定方案。
    priority: 26
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_agent_context_compression.token_budget
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个多轮规划 Agent 在第 8 轮后上下文逼近模型上限，开始遗漏早期约束。
        question_stem: 你会如何设计长会话 Agent 的上下文压缩、摘要和检索策略以稳住 token 预算？
        prompt_template: 结合候选人的 Agent 或 LLM 工程经验，生成一道技术深度题，要求说明 token 预算、摘要和检索取舍。
        scenario_skill_tags: [agent, context window, summarization, retrieval]
        resume_anchor_hints: [agent, context, summary, rag, llm]
        failure_categories: [missing_tradeoff, missing_edge_case, missing_action]
        rubric_additions: [说明 token 预算分配, 说明分层摘要与检索, 说明关键信息保留]
        expected_signals: [能给出 token 预算拆分方式, 能区分摘要丢弃与检索召回, 能识别哪些信息必须长期保留]
        anti_patterns: [简单截断早期消息, 全量喂入模型, 不评估摘要质量]
        good_answer_hints: [先量化 token 预算, 再分层摘要与检索, 最后做关键信息保护]
        priority: 17
        status: active
      - id: technical_depth.ai_agent_context_compression.summary_quality
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 自动摘要后 Agent 偶尔丢失约束（例如用户禁用某个工具），导致后续动作越界。
        question_stem: 如果摘要会丢失关键约束，你会如何评估摘要质量并防止 Agent 动作越界？
        prompt_template: 生成一道 Agent 摘要质量深挖题，要求候选人说明评估指标、约束保留与回归测试。
        scenario_skill_tags: [agent, summarization, evaluation, constraint preservation]
        resume_anchor_hints: [agent, summary, evaluation, constraint, regression]
        failure_categories: [missing_metrics, missing_edge_case, missing_observability]
        rubric_additions: [说明摘要质量指标, 说明硬约束单独存储, 说明回归测试与告警]
        expected_signals: [能定义关键约束保留率, 能把硬约束放到结构化存储, 能用回放测试检验越界]
        anti_patterns: [只看摘要长度, 不分离硬约束, 不做摘要回归测试]
        good_answer_hints: [先定义约束保留指标, 再分离硬约束存储, 最后做摘要回归]
        priority: 15
        status: active

  - id: technical_depth.ai_agent_memory_layers
    version: 1
    title: Agent 记忆层设计：短期 / 长期 / 工作记忆
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [agent, memory, working memory, vector store, llm]
    direction_tags: [internet_tech]
    role_tags: [ai_agent]
    rubric:
      must_cover: [记忆分层, 写入准入, 刷新策略, 冲突处理]
      minimum_bar: 能说明 Agent 短期上下文、工作记忆与长期记忆的职责边界。
      stretch: 能设计写入准入、过期、冲突合并与隐私边界。
    priority: 25
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_agent_memory_layers.write_gating
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个助手 Agent 把每轮对话都写入长期记忆，几周后召回噪音过大、影响回答质量。
        question_stem: 你会如何分层 Agent 的记忆，并设计长期记忆的写入准入与刷新策略？
        prompt_template: 结合候选人的 Agent / RAG 经验，生成一道技术深度题，要求说明记忆分层与写入准入。
        scenario_skill_tags: [agent, memory, vector store, write gating]
        resume_anchor_hints: [agent, memory, rag, embedding, retrieval]
        failure_categories: [missing_tradeoff, missing_edge_case, missing_action]
        rubric_additions: [说明记忆层职责, 说明写入准入规则, 说明刷新与过期策略]
        expected_signals: [能区分会话/工作/长期记忆, 能定义高价值写入条件, 能设置 TTL 与归档]
        anti_patterns: [全量对话写入, 不设过期, 不去重]
        good_answer_hints: [先分层定义职责, 再定写入门槛, 最后设刷新过期]
        priority: 16
        status: active
      - id: technical_depth.ai_agent_memory_layers.conflict_resolution
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 用户修改了一个长期偏好（例如不喜欢晨间提醒），但旧记忆仍然召回旧偏好。
        question_stem: 当 Agent 长期记忆出现新旧冲突时，你会如何设计合并、覆盖与用户可控编辑？
        prompt_template: 生成一道 Agent 记忆冲突深挖题，要求候选人说明合并策略、覆盖证据与用户可见性。
        scenario_skill_tags: [agent, memory conflict, merge, user editing]
        resume_anchor_hints: [agent, memory, conflict, merge, user]
        failure_categories: [missing_edge_case, missing_action, shallow_debugging]
        rubric_additions: [说明冲突识别方式, 说明新旧覆盖策略, 说明用户可见与可控]
        expected_signals: [能用时间戳/置信度合并冲突, 能让新事实覆盖旧偏好, 能提供查看与删除入口]
        anti_patterns: [简单追加导致回答漂移, 静默覆盖不留痕迹, 不提供用户删除入口]
        good_answer_hints: [先做冲突识别, 再定合并规则, 最后给用户可见与可控]
        priority: 14
        status: active

  - id: technical_depth.ai_fullstack_rag_indexing_strategy
    version: 1
    title: RAG 索引刷新策略：实时 / 批量 / 增量
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [ai fullstack, rag, indexing, freshness, cost]
    direction_tags: [internet_tech]
    role_tags: [ai_fullstack]
    rubric:
      must_cover: [新鲜度需求, 索引方式, 成本权衡, 一致性边界]
      minimum_bar: 能说明 RAG 索引在新鲜度与成本之间的取舍。
      stretch: 能结合增量更新、删除补偿、向量重建与监控形成闭环。
    priority: 26
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_fullstack_rag_indexing_strategy.fresh_vs_cost
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 内部知识库每天有数千次内容更新，RAG 检索需在 5 分钟内反映改动但成本受限。
        question_stem: 你会如何在新鲜度需求与索引成本之间取舍，设计 RAG 索引刷新策略？
        prompt_template: 结合候选人的 AI 全栈 / RAG 经验，生成一道技术深度题，要求说明实时/批量/增量取舍。
        scenario_skill_tags: [rag, indexing, freshness, cost, vector store]
        resume_anchor_hints: [rag, indexing, embedding, vector store, fullstack]
        failure_categories: [missing_tradeoff, missing_metrics, missing_edge_case]
        rubric_additions: [说明新鲜度 SLO, 说明三种刷新方式成本, 说明删除与失效处理]
        expected_signals: [能定义新鲜度 SLO 和测量, 能给出实时/批量/增量取舍, 能处理删除/失效]
        anti_patterns: [全量重建作为唯一方案, 只考虑写入不考虑删除, 不评估成本]
        good_answer_hints: [先定 SLO, 再设计混合刷新, 最后做删除补偿]
        priority: 17
        status: active
      - id: technical_depth.ai_fullstack_rag_indexing_strategy.delete_propagation
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 文档被删除后向量库仍有旧 embedding，导致 RAG 召回已不存在的内容。
        question_stem: 如果源数据频繁删除，你会如何在向量库与缓存层保证检索一致性？
        prompt_template: 生成一道 RAG 删除传播深挖题，要求候选人说明 tombstone、缓存失效与离线验证。
        scenario_skill_tags: [rag, deletion, tombstone, cache invalidation]
        resume_anchor_hints: [rag, vector store, cache, deletion, consistency]
        failure_categories: [missing_consistency_boundary, missing_edge_case, missing_observability]
        rubric_additions: [说明 tombstone 机制, 说明缓存失效路径, 说明离线一致性校验]
        expected_signals: [能用 tombstone 或 soft delete 防召回旧内容, 能传播失效到中间缓存, 能定期跑一致性巡检]
        anti_patterns: [只删源不删向量, 不管缓存, 不做一致性巡检]
        good_answer_hints: [先做 tombstone, 再传播失效, 最后定期巡检]
        priority: 15
        status: active

  - id: technical_depth.ai_fullstack_llm_streaming_degradation
    version: 1
    title: LLM API 流式响应、超时与降级
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [ai fullstack, llm, streaming, timeout, fallback]
    direction_tags: [internet_tech]
    role_tags: [ai_fullstack]
    rubric:
      must_cover: [流式协议, 超时分级, 降级路径, 用户体验]
      minimum_bar: 能说明 LLM API 流式调用的超时分级与失败降级。
      stretch: 能结合首 token 时延、断流恢复、备用模型与缓存形成完整体验。
    priority: 24
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_fullstack_llm_streaming_degradation.timeout_levels
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个 AI 写作助手在网络抖动时会出现长时间无响应，用户不知道是否要刷新页面。
        question_stem: 你会如何设计 LLM API 流式调用的超时分级与失败降级？
        prompt_template: 结合候选人的 AI 全栈 / 前后端经验，生成一道技术深度题，要求说明流式与降级。
        scenario_skill_tags: [llm, streaming, timeout, ux fallback]
        resume_anchor_hints: [llm, streaming, sse, timeout, frontend]
        failure_categories: [missing_action, missing_edge_case, missing_observability]
        rubric_additions: [说明首 token / 完成 token 不同超时, 说明断流恢复, 说明降级备用]
        expected_signals: [能区分首 token 与完成超时, 能在断流时提示并续传, 能切换备用模型或缓存]
        anti_patterns: [一个超时值打天下, 断流后直接报错, 没有备用模型]
        good_answer_hints: [先分超时层级, 再做断流恢复, 最后定降级备用]
        priority: 15
        status: active
      - id: technical_depth.ai_fullstack_llm_streaming_degradation.partial_output
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 模型流式输出到 70% 时被切断，已经渲染的内容不能直接丢，但也不能保留错误结尾。
        question_stem: 如果流式输出被中断只剩部分内容，你会如何在 UX 与数据层处理 partial output？
        prompt_template: 生成一道 LLM 部分输出处理深挖题，要求候选人说明 partial 内容标记、续写与日志。
        scenario_skill_tags: [llm, streaming, partial output, ux, logging]
        resume_anchor_hints: [llm, partial output, streaming, ux, frontend]
        failure_categories: [missing_edge_case, missing_action, missing_observability]
        rubric_additions: [说明 partial 状态标记, 说明续写或重试 UX, 说明日志与归因]
        expected_signals: [能标记 partial 状态不污染最终结果, 能提供续写或重试入口, 能记录中断点用于归因]
        anti_patterns: [partial 内容当成最终输出, 直接丢弃强制重来, 不留中断 trace]
        good_answer_hints: [先标 partial, 再提供续写, 最后留 trace]
        priority: 13
        status: active

  - id: technical_depth.ai_fullstack_typed_contract_chain
    version: 1
    title: AI 应用前后端类型契约的全链路
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [ai fullstack, typescript, schema, sdk, contract]
    direction_tags: [internet_tech]
    role_tags: [ai_fullstack]
    rubric:
      must_cover: [契约定义, 自动生成, 版本演进, 测试验证]
      minimum_bar: 能说明 AI 应用前后端类型契约如何在 schema → SDK → 组件之间一致。
      stretch: 能结合 OpenAPI / 自动生成 / 端到端测试形成完整契约链路。
    priority: 23
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_fullstack_typed_contract_chain.schema_sdk
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: AI 助手 API 频繁迭代，前端组件经常因后端字段改名而崩。
        question_stem: 你会如何用类型契约从 schema → SDK → 前端组件保证全链路类型一致？
        prompt_template: 结合候选人的 AI 全栈 / TypeScript 经验，生成一道技术深度题，要求说明类型契约链路。
        scenario_skill_tags: [typescript, schema, sdk, contract, fullstack]
        resume_anchor_hints: [typescript, schema, sdk, openapi, fullstack]
        failure_categories: [missing_action, missing_consistency_boundary, shallow_debugging]
        rubric_additions: [说明 schema 来源, 说明自动生成路径, 说明编译期检查]
        expected_signals: [能定义单一权威 schema, 能自动生成 SDK 与类型, 能在编译期阻断不兼容改动]
        anti_patterns: [前后端各自维护类型, 手写 SDK, 不做编译期检查]
        good_answer_hints: [先定 schema, 再自动生成, 最后编译期阻断]
        priority: 14
        status: active
      - id: technical_depth.ai_fullstack_typed_contract_chain.version_evolution
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 新版 API 想去掉一个字段，但旧版前端还在使用，必须无痛迁移。
        question_stem: 如果 AI 应用 API 字段要演进且不能中断旧前端，你会如何设计契约版本与迁移？
        prompt_template: 生成一道 AI 全栈契约演进深挖题，要求候选人说明版本策略、向后兼容与下线。
        scenario_skill_tags: [contract, versioning, backward compatibility, migration]
        resume_anchor_hints: [contract, version, migration, backward compatible, sdk]
        failure_categories: [missing_tradeoff, missing_action, missing_observability]
        rubric_additions: [说明版本策略, 说明向后兼容期, 说明下线监控]
        expected_signals: [能在 schema 中加 deprecation, 能保留过渡期, 能用监控判断下线时机]
        anti_patterns: [字段直接删除, 没有迁移期, 不监控旧调用]
        good_answer_hints: [先 deprecation, 再过渡期, 最后看监控下线]
        priority: 12
        status: active

  - id: technical_depth.ai_algorithm_finetune_vs_rag_vs_prompt
    version: 1
    title: 大模型微调 vs RAG vs Prompt 工程的选型与边界
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [ai algorithm, finetune, rag, prompt, selection]
    direction_tags: [internet_tech]
    role_tags: [ai_algorithm]
    rubric:
      must_cover: [问题特征, 三种方案边界, 成本与维护, 数据准备]
      minimum_bar: 能说明在什么场景选微调 / RAG / Prompt 工程。
      stretch: 能从数据规模、迭代速度、知识新鲜度、合规等多维度评估并给出混合方案。
    priority: 25
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_algorithm_finetune_vs_rag_vs_prompt.selection
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 业务方想做一个内部知识问答助手，知识每周更新且数据较少。
        question_stem: 你会如何评估并选择微调 / RAG / Prompt 工程的方案组合？
        prompt_template: 结合候选人的 AI 算法 / LLM 经验，生成一道技术深度题，要求说明选型决策。
        scenario_skill_tags: [llm, finetune, rag, prompt, selection]
        resume_anchor_hints: [llm, finetune, rag, prompt, algorithm]
        failure_categories: [missing_tradeoff, missing_action, missing_metrics]
        rubric_additions: [说明问题特征评估, 说明三种方案边界, 说明成本与维护]
        expected_signals: [能从数据规模/新鲜度/合规等维度评估, 能给出主从混合方案, 能说明长期维护成本]
        anti_patterns: [一切都微调, 一切都 RAG, 只看效果不看成本]
        good_answer_hints: [先看问题特征, 再比方案边界, 最后做组合]
        priority: 14
        status: active
      - id: technical_depth.ai_algorithm_finetune_vs_rag_vs_prompt.hybrid_design
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 业务后期希望"既要 RAG 实时新鲜，又要微调风格统一"。
        question_stem: 如果要在同一系统里组合微调 + RAG + Prompt 工程，你会如何设计协作边界？
        prompt_template: 生成一道大模型组合方案深挖题，要求候选人说明协作分工与评估。
        scenario_skill_tags: [llm, hybrid, finetune, rag, prompt]
        resume_anchor_hints: [llm, hybrid, finetune, rag, prompt]
        failure_categories: [missing_tradeoff, missing_action, missing_metrics]
        rubric_additions: [说明分工边界, 说明评估指标, 说明迭代节奏]
        expected_signals: [能让微调负责风格/RAG 负责知识/Prompt 负责动态指令, 能定义协作评估, 能定迭代节奏]
        anti_patterns: [三者无明确分工, 不定评估, 不分迭代节奏]
        good_answer_hints: [先分工, 再评估, 最后定节奏]
        priority: 12
        status: active

  - id: technical_depth.ai_algorithm_feature_vs_representation
    version: 1
    title: 特征工程 vs 表示学习的选型
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [ai algorithm, feature engineering, representation learning, modeling]
    direction_tags: [internet_tech]
    role_tags: [ai_algorithm]
    rubric:
      must_cover: [数据特征, 模型容量, 解释性, 迭代成本]
      minimum_bar: 能说明特征工程与表示学习在什么场景更合适。
      stretch: 能从数据量、解释性、迭代速度等给出明确取舍。
    priority: 23
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.ai_algorithm_feature_vs_representation.selection
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个推荐场景数据量中等，业务方要求"模型可解释"。
        question_stem: 你会如何在特征工程与表示学习之间做选型？
        prompt_template: 结合候选人的 AI 算法经验，生成一道技术深度题，要求说明选型决策。
        scenario_skill_tags: [feature engineering, representation learning, modeling]
        resume_anchor_hints: [feature, representation, model, algorithm]
        failure_categories: [missing_tradeoff, missing_action, missing_metrics]
        rubric_additions: [说明数据特征评估, 说明模型容量评估, 说明解释性需求]
        expected_signals: [能根据数据量决定模型容量, 能根据解释性需求选方向, 能给出迭代成本估算]
        anti_patterns: [无脑用深度模型, 不评估解释性, 不算迭代成本]
        good_answer_hints: [先评估数据, 再评估解释性, 最后估迭代成本]
        priority: 13
        status: active
      - id: technical_depth.ai_algorithm_feature_vs_representation.hybrid
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 现有线性模型效果到达瓶颈，团队想引入深度网络但担心黑盒化。
        question_stem: 如果要把表示学习增量接入到现有特征工程模型，你会如何设计与评估？
        prompt_template: 生成一道特征/表示混合模型深挖题，要求候选人说明逐步接入与对照评估。
        scenario_skill_tags: [feature engineering, representation learning, hybrid model]
        resume_anchor_hints: [feature, representation, hybrid, model, evaluation]
        failure_categories: [missing_metrics, missing_action, missing_tradeoff]
        rubric_additions: [说明逐步接入路径, 说明对照评估, 说明可解释保留]
        expected_signals: [能让表示学习作为补充特征逐步接入, 能用 A/B 对照评估增量, 能保留可解释性入口]
        anti_patterns: [一次性全换深度模型, 不做对照, 牺牲所有解释性]
        good_answer_hints: [先增量接入, 再对照评估, 最后保留可解释]
        priority: 11
        status: active

  - id: technical_depth.frontend_web_render_pipeline
    version: 1
    title: 浏览器渲染管线与性能优化
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [frontend, browser, render, layout, paint, performance]
    direction_tags: [internet_tech]
    role_tags: [frontend_web]
    rubric:
      must_cover: [渲染阶段, 关键路径, 性能预算, 优化手段]
      minimum_bar: 能说明浏览器从 HTML 到像素的关键阶段与性能瓶颈。
      stretch: 能结合 Core Web Vitals、INP/LCP/CLS 给出系统化优化路径。
    priority: 26
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.frontend_web_render_pipeline.critical_path
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个落地页 LCP 3.2 秒、CLS 0.18，业务方要求两周内压到 LCP < 1.8 / CLS < 0.05。
        question_stem: 你会如何系统化优化浏览器渲染关键路径与核心指标？
        prompt_template: 结合候选人的前端 / 性能经验，生成一道技术深度题，要求说明渲染管线与 Core Web Vitals。
        scenario_skill_tags: [frontend, render, lcp, cls, performance]
        resume_anchor_hints: [frontend, render, performance, lcp, web vitals]
        failure_categories: [missing_metrics, missing_action, missing_tradeoff]
        rubric_additions: [说明关键渲染路径, 说明核心指标拆解, 说明可量化优化动作]
        expected_signals: [能拆 LCP/CLS/INP 三类指标, 能给出关键路径优化清单, 能区分布局抖动与首屏阻塞]
        anti_patterns: [只说"加缓存", 不拆指标, 没量化目标]
        good_answer_hints: [先拆指标, 再分阶段优化, 最后做监控]
        priority: 16
        status: active
      - id: technical_depth.frontend_web_render_pipeline.layout_thrash
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 列表滚动时频繁触发 layout 重计算，低端设备出现明显卡顿。
        question_stem: 如果遇到大列表 layout thrashing，你会如何在 DOM 与样式两层定位并优化？
        prompt_template: 生成一道布局抖动深挖题，要求候选人说明读写分离、虚拟列表与样式隔离。
        scenario_skill_tags: [frontend, layout thrashing, virtual list, css containment]
        resume_anchor_hints: [frontend, layout, virtual list, performance, css]
        failure_categories: [shallow_debugging, missing_metrics, missing_tradeoff]
        rubric_additions: [说明读写分离原理, 说明虚拟列表/容器化, 说明样式隔离]
        expected_signals: [能用 layout/forced reflow 工具定位, 能用虚拟列表/css contain 减少重算, 能区分读写顺序]
        anti_patterns: [只用 transform 改, 不分析根因, 不做样式隔离]
        good_answer_hints: [先抓 forced reflow, 再做读写分离, 最后用虚拟化]
        priority: 14
        status: active

  - id: technical_depth.frontend_web_state_management
    version: 1
    title: 大型 SPA 状态管理选型与边界
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [frontend, react, state management, redux, zustand]
    direction_tags: [internet_tech]
    role_tags: [frontend_web]
    rubric:
      must_cover: [状态分类, 选型框架, 边界划分, 测试策略]
      minimum_bar: 能区分组件级 / 跨组件 / 全局状态并给出选型。
      stretch: 能从可测试性、SSR 兼容、并发渲染等维度评估。
    priority: 25
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.frontend_web_state_management.layer_selection
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个中后台 SPA 涉及表单 / 列表 / 全局会话 / 多窗口同步等多种状态。
        question_stem: 你会如何分层管理这些状态并给出技术选型框架？
        prompt_template: 结合候选人的 React / 状态管理经验，生成一道技术深度题，要求说明分层与选型。
        scenario_skill_tags: [frontend, state management, react, scope]
        resume_anchor_hints: [frontend, state management, redux, zustand, react]
        failure_categories: [missing_tradeoff, missing_action, missing_edge_case]
        rubric_additions: [说明状态分类, 说明选型框架, 说明边界划分]
        expected_signals: [能区分本地/跨组件/全局/服务器状态, 能给出选型决策树, 能避免状态污染]
        anti_patterns: [一切都放 redux, 不分类乱用, 全 props drilling]
        good_answer_hints: [先分类状态, 再选工具, 最后划边界]
        priority: 14
        status: active
      - id: technical_depth.frontend_web_state_management.concurrent_rendering
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 升级到 React 18 并发模式后某些状态更新出现重复渲染与状态撕裂。
        question_stem: 如果状态管理在并发渲染下出现一致性问题，你会如何定位并设计稳定方案？
        prompt_template: 生成一道并发渲染状态一致性深挖题，要求候选人说明 useSyncExternalStore、过渡更新等。
        scenario_skill_tags: [react, concurrent, state, sync external store]
        resume_anchor_hints: [react, concurrent, state, react 18, tearing]
        failure_categories: [missing_consistency_boundary, shallow_debugging, missing_tradeoff]
        rubric_additions: [说明撕裂现象, 说明外部 store 同步, 说明过渡更新]
        expected_signals: [能用 useSyncExternalStore 解决撕裂, 能区分紧急/过渡更新, 能用回归测试验证]
        anti_patterns: [回退非并发模式, 频繁手动 flushSync, 不写回归]
        good_answer_hints: [先识别撕裂, 再用同步 API, 最后做回归]
        priority: 12
        status: active

  - id: technical_depth.frontend_web_build_engineering
    version: 1
    title: 前端工程化：模块化、构建链与代码拆分
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [frontend, build, webpack, vite, code splitting, tree shaking]
    direction_tags: [internet_tech]
    role_tags: [frontend_web]
    rubric:
      must_cover: [模块化策略, 构建链选型, 代码拆分, 体积/性能权衡]
      minimum_bar: 能说明前端构建链的模块化与代码拆分基本能力。
      stretch: 能结合包体积分析、长缓存、按需加载形成完整工程方案。
    priority: 23
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.frontend_web_build_engineering.bundle_diet
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个产品首页 JS bundle 2.4MB，移动端首屏体感慢。
        question_stem: 你会如何系统化优化前端 bundle 体积并保持长期可控？
        prompt_template: 结合候选人的前端工程化经验，生成一道技术深度题，要求说明 bundle 优化全链路。
        scenario_skill_tags: [frontend, bundle, code splitting, tree shaking, build]
        resume_anchor_hints: [frontend, bundle, webpack, vite, build]
        failure_categories: [missing_metrics, missing_action, missing_tradeoff]
        rubric_additions: [说明分析方法, 说明拆分策略, 说明长期治理]
        expected_signals: [能用 bundle analyzer 定位大头, 能按路由/特性拆分, 能定 CI 体积守护]
        anti_patterns: [只升级框架版本, 不分析根因, 没有体积守护]
        good_answer_hints: [先分析体积, 再做拆分, 最后建守护]
        priority: 13
        status: active
      - id: technical_depth.frontend_web_build_engineering.cache_strategy
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 长缓存策略下小改动导致整个 vendor chunk 失效，CDN 回源压力上升。
        question_stem: 如果长缓存失效导致 CDN 压力上升，你会如何设计稳定的 hash / chunk 策略？
        prompt_template: 生成一道前端长缓存与 chunk 策略深挖题，要求候选人说明 content hash、稳定 chunk 划分。
        scenario_skill_tags: [frontend, long term cache, content hash, chunk strategy]
        resume_anchor_hints: [frontend, cache, hash, chunk, build]
        failure_categories: [shallow_debugging, missing_action, missing_metrics]
        rubric_additions: [说明 hash 类型, 说明 chunk 边界, 说明回归监控]
        expected_signals: [能区分 fullhash/chunkhash/contenthash, 能稳定 vendor/common chunk 边界, 能监控失效率]
        anti_patterns: [全部 fullhash, 不分 vendor, 不监控失效]
        good_answer_hints: [先稳定 hash, 再分 chunk, 最后监控]
        priority: 11
        status: active

  - id: technical_depth.sre_linux_tuning
    version: 1
    title: Linux 系统调优（CPU/内存/I/O/网络）
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [sre, linux, kernel, performance, tuning]
    direction_tags: [internet_tech]
    role_tags: [sre]
    rubric:
      must_cover: [指标识别, 工具链, 瓶颈定位, 长期治理]
      minimum_bar: 能用 top/vmstat/iostat/sar 等定位 Linux 性能瓶颈。
      stretch: 能结合 eBPF / perf / 火焰图 做深度剖析与治理。
    priority: 26
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.sre_linux_tuning.bottleneck
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一台业务机 CPU usr 突然飙到 90% 持续 30 分钟，但应用日志看不出异常。
        question_stem: 你会如何用 Linux 工具链定位这类 CPU / 内存 / I/O / 网络瓶颈？
        prompt_template: 结合候选人的 SRE / Linux 经验，生成一道技术深度题，要求说明工具链与诊断思路。
        scenario_skill_tags: [sre, linux, tuning, debugging]
        resume_anchor_hints: [sre, linux, tuning, performance, debugging]
        failure_categories: [shallow_debugging, missing_metrics, missing_action]
        rubric_additions: [说明指标识别, 说明工具链, 说明瓶颈定位]
        expected_signals: [能按 CPU/IO/网络分维度选工具, 能用 perf / eBPF 做深度剖析, 能给出长期治理建议]
        anti_patterns: [只看 top 重启服务, 不分维度, 没长期治理]
        good_answer_hints: [先识别指标, 再选工具, 最后做治理]
        priority: 14
        status: active
      - id: technical_depth.sre_linux_tuning.iowait
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 数据库实例 iowait 飙升导致接口延迟翻倍。
        question_stem: 如果 Linux iowait 异常持续上升，你会如何分层定位并优化？
        prompt_template: 生成一道 Linux iowait 诊断深挖题，要求候选人说明 I/O 子系统的分层定位。
        scenario_skill_tags: [sre, linux, iowait, storage, kernel]
        resume_anchor_hints: [sre, linux, iowait, storage, perf]
        failure_categories: [shallow_debugging, missing_metrics, missing_action]
        rubric_additions: [说明 I/O 路径分层, 说明工具选择, 说明优化方向]
        expected_signals: [能区分 user/kernel/disk 层瓶颈, 能用 iostat/blktrace 定位, 能给出磁盘/调度器/缓存层优化]
        anti_patterns: [只换硬盘, 不分层, 不做长期监控]
        good_answer_hints: [先分层定位, 再选工具, 最后优化]
        priority: 12
        status: active

  - id: technical_depth.sre_kubernetes_ops
    version: 1
    title: Kubernetes 集群运维：Pod / 调度 / 网络策略
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [sre, kubernetes, scheduling, network policy, ops]
    direction_tags: [internet_tech]
    role_tags: [sre]
    rubric:
      must_cover: [Pod 生命周期, 调度策略, 网络策略, 运维工具]
      minimum_bar: 能解释 Kubernetes 核心运维要点。
      stretch: 能结合 HPA / VPA / Cluster Autoscaler / NetworkPolicy / Operator 形成自动化运维。
    priority: 25
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.sre_kubernetes_ops.daily_ops
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个新接手的 K8s 集群，常出现 Pod 调度失败 / 网络不通 / 健康检查抖动。
        question_stem: 你会如何系统化排查 / 治理 Kubernetes 集群的运维问题？
        prompt_template: 结合候选人的 SRE / K8s 经验，生成一道技术深度题，要求说明运维要点。
        scenario_skill_tags: [sre, kubernetes, ops, debugging]
        resume_anchor_hints: [sre, kubernetes, k8s, ops, devops]
        failure_categories: [shallow_debugging, missing_action, missing_observability]
        rubric_additions: [说明常见现象, 说明根因分类, 说明治理动作]
        expected_signals: [能区分调度/网络/健康三类问题, 能用 kubectl / events / logs 定位, 能给出治理动作]
        anti_patterns: [只重启 Pod, 不查 events, 没治理]
        good_answer_hints: [先分类, 再定位, 最后治理]
        priority: 13
        status: active
      - id: technical_depth.sre_kubernetes_ops.scheduling_optimization
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 集群资源利用率不均：部分节点高负载，部分节点闲置。
        question_stem: 如果 K8s 调度不均导致资源浪费，你会如何用调度策略优化？
        prompt_template: 生成一道 K8s 调度优化深挖题，要求候选人说明调度策略与亲和性。
        scenario_skill_tags: [sre, kubernetes, scheduling, affinity, optimization]
        resume_anchor_hints: [sre, kubernetes, scheduling, affinity]
        failure_categories: [missing_action, missing_tradeoff, missing_metrics]
        rubric_additions: [说明现状测量, 说明调度策略, 说明效果验证]
        expected_signals: [能用资源指标定位不均, 能选 affinity / topology spread / VPA, 能用指标验证效果]
        anti_patterns: [只加节点, 不分析根因, 不验证]
        good_answer_hints: [先测量, 再选策略, 最后验证]
        priority: 11
        status: active

  - id: technical_depth.sre_cloud_native_ops
    version: 1
    title: 云原生运维：负载均衡 / 自动扩容 / 故障转移
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [sre, cloud native, load balancing, autoscaling, failover]
    direction_tags: [internet_tech]
    role_tags: [sre]
    rubric:
      must_cover: [负载均衡, 自动扩容, 故障转移, 健康检查]
      minimum_bar: 能说明云原生基础设施的关键能力。
      stretch: 能结合多区域 / 多可用区 / Service Mesh 设计完整高可用。
    priority: 23
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.sre_cloud_native_ops.lb_autoscale
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个新接入的云服务在突发流量下出现节点过载、扩容来不及。
        question_stem: 你会如何系统化设计云原生服务的负载均衡和自动扩容机制？
        prompt_template: 结合候选人的 SRE / 云原生经验，生成一道技术深度题，要求说明 LB 与扩容。
        scenario_skill_tags: [sre, cloud native, lb, autoscaling]
        resume_anchor_hints: [sre, cloud, lb, autoscaling, aws, azure]
        failure_categories: [missing_action, missing_metrics, missing_tradeoff]
        rubric_additions: [说明 LB 策略, 说明扩容触发, 说明健康检查]
        expected_signals: [能选合适 LB 算法, 能定义合理扩容触发, 能用健康检查避免雪崩]
        anti_patterns: [只看 CPU 扩容, 不分 LB 策略, 不做健康检查]
        good_answer_hints: [先选 LB, 再定扩容, 最后做健康]
        priority: 12
        status: active
      - id: technical_depth.sre_cloud_native_ops.cross_az_failover
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 一个可用区故障导致服务下线 30 分钟，业务要求"再不出现这种问题"。
        question_stem: 如果要做跨可用区 / 跨区域故障转移，你会如何设计架构与演练？
        prompt_template: 生成一道跨区域故障转移深挖题，要求候选人说明架构与演练。
        scenario_skill_tags: [sre, cross az, failover, ha, drills]
        resume_anchor_hints: [sre, cross az, failover, disaster recovery]
        failure_categories: [missing_action, missing_failure_mode, missing_tradeoff]
        rubric_additions: [说明架构设计, 说明流量切换, 说明定期演练]
        expected_signals: [能用多 AZ/多区域部署, 能设计快速流量切换, 能用混沌演练验证]
        anti_patterns: [单 AZ 部署, 无切换预案, 不做演练]
        good_answer_hints: [先多 AZ, 再做切换, 最后演练]
        priority: 10
        status: active

  - id: technical_depth.mobile_platform_lifecycle
    version: 1
    title: Android / iOS 平台生命周期与状态保存
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [mobile, android, ios, lifecycle, state restoration]
    direction_tags: [internet_tech]
    role_tags: [mobile]
    rubric:
      must_cover: [生命周期, 状态保存, 系统回收, 恢复策略]
      minimum_bar: 能解释移动端 Activity / ViewController 生命周期与状态保存。
      stretch: 能结合后台进程回收 / 内存压力 / 跨进程恢复给出完整方案。
    priority: 25
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.mobile_platform_lifecycle.state_restoration
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个移动应用用户切回后台 30 分钟后回来，发现填了一半的表单丢失。
        question_stem: 你会如何设计移动端生命周期与状态保存以防止此类问题？
        prompt_template: 结合候选人的 Android / iOS 经验，生成一道技术深度题，要求说明生命周期与状态保存。
        scenario_skill_tags: [mobile, lifecycle, state restoration, savedinstancestate]
        resume_anchor_hints: [mobile, android, ios, lifecycle, state]
        failure_categories: [missing_edge_case, missing_action, missing_consistency_boundary]
        rubric_additions: [说明生命周期, 说明状态保存, 说明系统回收恢复]
        expected_signals: [能用 onSaveInstanceState / state restoration 保存, 能区分进程被杀和暂停, 能恢复关键数据]
        anti_patterns: [全用全局单例, 不区分回收, 不做恢复测试]
        good_answer_hints: [先保关键状态, 再分场景恢复, 最后测试]
        priority: 11
        status: active
      - id: technical_depth.mobile_platform_lifecycle.deep_link
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 应用通过 deep link 启动时偶尔出现页面栈混乱。
        question_stem: 如果 deep link 启动导致页面栈不一致，你会如何在生命周期层修复？
        prompt_template: 生成一道 deep link 与页面栈深挖题，要求候选人说明导航栈管理。
        scenario_skill_tags: [mobile, deep link, navigation stack, intent flag]
        resume_anchor_hints: [mobile, deep link, navigation, intent]
        failure_categories: [missing_edge_case, missing_action, missing_consistency_boundary]
        rubric_additions: [说明启动模式, 说明栈管理, 说明状态对齐]
        expected_signals: [能选合适 launch mode / scene, 能用栈管理避免错乱, 能在恢复时对齐状态]
        anti_patterns: [全 standard 启动, 不管栈, 不测 deep link]
        good_answer_hints: [先选启动模式, 再管栈, 最后对齐]
        priority: 9
        status: active

  - id: technical_depth.mobile_cross_platform_tradeoff
    version: 1
    title: Flutter / React Native 跨端框架的取舍
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [mobile, flutter, react native, cross platform, tradeoff]
    direction_tags: [internet_tech]
    role_tags: [mobile]
    rubric:
      must_cover: [适用场景, 性能差异, 工程成本, 演进策略]
      minimum_bar: 能比较原生 / Flutter / RN 三种方案。
      stretch: 能结合团队能力 / 项目规模 / 长期维护给出选型框架。
    priority: 23
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.mobile_cross_platform_tradeoff.selection
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 一个新产品要决定选原生还是 Flutter / RN，团队倾向跨端但担心性能。
        question_stem: 你会如何评估并选择 Flutter / RN / 原生方案？
        prompt_template: 结合候选人的移动端 / 跨端经验，生成一道技术深度题，要求说明选型框架。
        scenario_skill_tags: [mobile, flutter, react native, cross platform]
        resume_anchor_hints: [mobile, flutter, react native, cross platform]
        failure_categories: [missing_tradeoff, missing_action, missing_metrics]
        rubric_additions: [说明适用场景, 说明性能差异, 说明工程成本]
        expected_signals: [能给场景适用矩阵, 能比较性能/包体/渲染差异, 能评估团队能力与维护成本]
        anti_patterns: [盲目跟新技术, 不评估团队能力, 不算长期成本]
        good_answer_hints: [先评估场景, 再比性能, 最后算成本]
        priority: 11
        status: active
      - id: technical_depth.mobile_cross_platform_tradeoff.migration
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 现有 RN 项目某些模块性能不达标，团队想替换为原生。
        question_stem: 如果要把跨端项目部分模块迁回原生，你会如何设计混合架构？
        prompt_template: 生成一道跨端混合架构深挖题，要求候选人说明混合方案与边界。
        scenario_skill_tags: [mobile, hybrid architecture, react native, native module]
        resume_anchor_hints: [mobile, hybrid, react native, native module]
        failure_categories: [missing_action, missing_tradeoff, missing_consistency_boundary]
        rubric_additions: [说明混合边界, 说明通信机制, 说明回滚策略]
        expected_signals: [能用 bridge / module 实现混合, 能定通信协议, 能给降级与回滚]
        anti_patterns: [全量替换风险大, 通信无契约, 没回滚]
        good_answer_hints: [先定边界, 再通信, 最后回滚]
        priority: 9
        status: active

  - id: technical_depth.mobile_native_module_integration
    version: 1
    title: 原生模块集成（相机 / 蓝牙 / 推送）的工程边界
    dimension: technical_depth
    job_levels: [junior, mid, senior]
    skill_tags: [mobile, native module, camera, bluetooth, push]
    direction_tags: [internet_tech]
    role_tags: [mobile]
    rubric:
      must_cover: [权限管理, 兼容差异, 失败处理, 用户体验]
      minimum_bar: 能集成原生硬件 / 系统能力并处理常见兼容问题。
      stretch: 能给出权限申请 / 兼容降级 / 失败处理的完整方案。
    priority: 21
    status: active
    source: manual_yaml
    scope: global
    language: zh-CN
    variants:
      - id: technical_depth.mobile_native_module_integration.permission
        version: 1
        intent: opening
        difficulty: standard
        scenario_brief: 应用集成相机后，部分用户在权限拒绝后无法再次使用功能。
        question_stem: 你会如何系统化设计原生能力（如相机）的权限申请与失败处理？
        prompt_template: 结合候选人的 Android / iOS 经验，生成一道技术深度题，要求说明权限与失败处理。
        scenario_skill_tags: [mobile, permission, camera, native, ux]
        resume_anchor_hints: [mobile, permission, camera, native]
        failure_categories: [missing_edge_case, missing_action, missing_observability]
        rubric_additions: [说明权限流程, 说明拒绝处理, 说明引导恢复]
        expected_signals: [能合理选择申请时机, 能区分首次/二次/永久拒绝, 能引导用户开启权限]
        anti_patterns: [启动就要权限, 拒绝后无引导, 没失败处理]
        good_answer_hints: [先时机, 再分情况, 最后引导]
        priority: 10
        status: active
      - id: technical_depth.mobile_native_module_integration.compatibility
        version: 1
        intent: deep_probe
        difficulty: deep_probe
        scenario_brief: 蓝牙模块在部分老 Android 设备上无法稳定工作。
        question_stem: 如果原生能力存在设备/系统版本兼容差异，你会如何在工程上稳定支持？
        prompt_template: 生成一道原生模块兼容深挖题，要求候选人说明分层适配与降级。
        scenario_skill_tags: [mobile, compatibility, bluetooth, fallback]
        resume_anchor_hints: [mobile, compatibility, bluetooth, fallback]
        failure_categories: [missing_edge_case, missing_action, missing_tradeoff]
        rubric_additions: [说明兼容矩阵, 说明分层适配, 说明降级策略]
        expected_signals: [能建兼容矩阵, 能按版本/厂商分层适配, 能给降级体验]
        anti_patterns: [只测主流设备, 不分层, 不降级]
        good_answer_hints: [先建矩阵, 再适配, 最后降级]
        priority: 8
        status: active
